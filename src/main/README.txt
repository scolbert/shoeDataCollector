Testing
  Unit Tests must end with "Test"
  Integration Tests must end with "IT"
  If you run tests by clicking on the file in your IDE, you will run all tests in the project
  If you use mvn test, you will run only the unit tests
  If you use mvn verify you will run only the integration tests